# nfc-access-control
A simulation-first NFC access control system built with Python, featuring whitelist/blacklist rules, access logging with timestamps, and a REST API. Supports real NFC readers via nfcpy, but runs fully without NFC hardware.
